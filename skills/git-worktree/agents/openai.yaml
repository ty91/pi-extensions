interface:
  display_name: "Git Worktree"
  short_description: "Safely create and manage Git worktrees"
  default_prompt: "Use $git-worktree to create a worktree for my feature branch."
policy:
  allow_implicit_invocation: true
